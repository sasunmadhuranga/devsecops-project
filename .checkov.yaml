# .checkov.yaml — project-level policy overrides
# Each skip must be justified.

soft-fail: false
framework:
  - terraform
directory:
  - terraform/

skip-check:
  # CKV_AWS_91: ALB access logging — FIXED: now logging to S3
  # CKV_AWS_2:  ALB HTTP listener — demo project has no domain/ACM cert.
  #             In production: add ACM cert and redirect port 80 → 443.
  - CKV_AWS_2
  # CKV_AWS_150: ALB deletion protection — intentionally off for easy teardown in demo
  - CKV_AWS_150
  # CKV_AWS_131: ALB drop invalid headers — add when HTTPS listener is configured
  - CKV_AWS_131
  # CKV_AWS_338: CloudWatch log retention — FIXED: now 30 days.
  #              In production increase to 90+ days for compliance.
